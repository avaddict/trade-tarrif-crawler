@Library('pipeline-library') _

javaPipeline {
    SERVICE_NAME = "${artifactId}"
    SONARQUBE_PROJECT_NAME = "Imports-${artifactId}"
    SERVICE_VERSION = "1.0.${BUILD_NUMBER}"
    ENVIRONMENT = "Sandpit"
    SELENIUM_BRANCH = "master"
}
